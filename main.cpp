#include "Activity.h"
#include <iostream>

int main()
{
    Activity a; // my building test here, cmake is ok now, please ignore it
    for (int i = 0; i < 30; i++)
    {
        for (int j = 0; j < 60; j++)
        {
            // int f = 0;
            // if (i % 2 == 0)
            //     f++;
            // if (j % 2 == 0)
            //     f++;
            // if (f == 2)
            //     std::cout << '+';
            // else if (f == 0)
            //     std::cout << '*';
            // else if (i % 2 == 0)
            //     std::cout << '-';
            // else
            //     std::cout << "|";
            // std::cout << "草";
            // std::cout << RED << "*" << RESET;
            // std::cout << 'o';
        }
        // std::cout << std::endl;
    }

    return 0;
}